version: '3.9'
services:
  frontend:
    build:
      dockerfile: ./docker/Dockerfile.frontend
    command: 'npm run build'
    ports:
      - '3000:3000'
    volumes:
      - ./:/app
      - /app/node_modules
